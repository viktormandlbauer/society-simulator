name: $(BuildID)

trigger:
  branches:
    include:
      - main
  paths:
    include:
      - 'gamestate/*'

pool:
  vmImage: 'ubuntu-latest'

steps:
  - script: |
      echo "Hello from 'gamestate'"